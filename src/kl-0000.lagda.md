```
module kl-0000 where

open import MLTT.Spartan hiding (_+_)

open import CommRing
open import Module
open import Algebra
open import LinearCategory
```

```
algebra-is-one-object-linear-category : (K : CommRing 𝓤) → (A : Algebra 𝓤 K) → linear-precategory 𝓥 𝓤 K
algebra-is-one-object-linear-category {𝓤}{𝓥} K A = record { str = str ; ax = axioms }
  where
  open category-structure
  open linear-precategory-axioms
  open comm-ring-on (K .pr₂) renaming (_*_ to _*ᴷ_; _+_ to _+ᴷ_)
  open algebra-on (A .pr₂)
  open module-on (algebra-on.module-str (A .pr₂))
  open module-axioms (module-on.is-module (algebra-on.module-str (A .pr₂)))
  open algebra-axioms (algebra-on.is-algebra (A .pr₂))

  str : category-structure 𝓥 𝓤
  str .ob = 𝟙
  str .hom _ _ = ⟨ A ⟩
  str .idn _ = 1a
  str ._⨾_ f g = f * g

  axioms : linear-precategory-axioms K str
  axioms .homMod _ _ = module-str
  axioms .idn-L f = *-neuL
  axioms .idn-R f = *-neuR
  axioms .assoc f g h = *-assoc
  axioms .linearL a b c = is-algebra .algebra-axioms.linearL
  axioms .linearR a b c = is-algebra .algebra-axioms.linearR
  axioms .compatible r s a b = is-algebra .algebra-axioms.compatible
```
